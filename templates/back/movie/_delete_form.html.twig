<div class="container pt-5">
	<div class="row mb-2">
		<div class="col">
			<form method="post" action="{{ path('app_back_movie_delete', {'id': movie.id}) }}" 
				onsubmit="return confirm('Are you sure you want to delete this item?');">
				<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movie.id) }}">
				<button class="btn btn-danger float-end">Supprimer</button>
			</form>
		</div>
	</div>
</div>
